# ===== Core orchestration =====
langgraph>=0.2.50            # state machine for the voice flow
langchain-core>=0.3.20       # base primitives used by langgraph

# ===== Groq API (STT + LLM) =====
groq>=0.13.0                 # official Groq SDK — whisper-large-v3 + llama-3.1-8b-instant

# ===== UI =====
streamlit>=1.39.0            # browser dashboard (ui/app.py)
streamlit-autorefresh>=1.0.1 # poll tmp/state.json without manual reruns

# ===== Audio capture + VAD =====
sounddevice>=0.5.0           # cross-platform mic capture (lighter than PyAudio, no PortAudio headers needed on most systems)
numpy>=1.26.0                # audio buffer math
scipy>=1.13.0                # WAV encode for Groq upload
webrtcvad>=2.0.10            # silence detection — 10/20/30 ms frame VAD

# ===== Keyboard push-to-talk =====
pynput>=1.7.7                # SPACE push-to-talk listener (works headless, unlike `keyboard`)

# ===== Fuzzy command matching =====
rapidfuzz>=3.10.0            # allowlist matching for execute_cmd (fast, MIT-licensed)

# ===== Config + secrets =====
python-dotenv>=1.0.1         # load .env via load_dotenv()
pydantic>=2.9.0              # typed settings + classify_node JSON validation
pydantic-settings>=2.6.0     # config/settings.py model

# ===== Target agent =====
aider-chat>=0.60.0           # the terminal coding agent we're wrapping

# ===== Dev / tests =====
pytest>=8.3.0
pytest-asyncio>=0.24.0